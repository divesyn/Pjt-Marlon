import { NaoDisponivelPipe } from './nao-disponivel.pipe';

describe('NaoDisponivelPipe', () => {
  it('create an instance', () => {
    const pipe = new NaoDisponivelPipe();
    expect(pipe).toBeTruthy();
  });
});
